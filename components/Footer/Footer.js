import React from "react"
import styles from './Footer.module.scss'

export default function Footer(props) {
    return (
        <div class={styles.Footer}>
            <h1 class={styles.title}>footer</h1>
        </div>
    )
}